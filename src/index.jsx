import React from "react";
import ReactDOM from "react-dom/client";
import App from "./swapi_ui.jsx";

const rootEl = document.getElementById('main');
const root = ReactDOM.createRoot(rootEl);
root.render(<App />);

// ============================================================================================================
